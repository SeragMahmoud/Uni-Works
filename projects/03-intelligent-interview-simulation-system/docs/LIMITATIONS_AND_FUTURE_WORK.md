# Limitations And Future Work

## Limitations

- This public folder is a case study, not a runnable mirror.
- Private `.env`, logs, generated files, model binaries, datasets, archives, and identifiable webcam media are excluded.
- AI backend behavior and deployment status were not independently verified for this public package.

## Future Work

- Publish a mock AI backend demo.
- Add anonymized sample WebSocket event transcripts.
- Add sanitized JSON examples for interview feedback.
- Add public-safe exported diagrams for each subsystem.
- Add a small frontend-only walkthrough with static data.
